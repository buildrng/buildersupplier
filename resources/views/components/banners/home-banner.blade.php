<div class="mb-12 md:mb-14 xl:mb-16">
    <div class="grid grid-cols-1 lg:grid-cols-7 2xl:grid-cols-9 gap-5 xl:gap-7 lg:gap-y-14">
        {{-- <x-banners.sub-categories /> --}}
        {{-- <div class="col-span-full lg:col-span-5 xl:col-span-5 row-span-full lg:row-auto grid grid-cols-2 gap-2 md:gap-3.5 lg:gap-5 xl:gap-7">
            {banners.map((banner: any) => (
                <BannerCard
                    key={`banner--key${banner.id}`}
                    banner={banner}
                    href={`${ROUTES.COLLECTIONS}/${banner.slug}`}
                    class={banner.type === "large" ? "col-span-2" : "col-span-1"}
                />
            ))}
        </div> --}}
    </div>
</div>